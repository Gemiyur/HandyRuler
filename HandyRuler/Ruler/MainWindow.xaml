<Window x:Class="Ruler.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ruler"
        mc:Ignorable="d"
        x:Name="mainWindow"
        AllowsTransparency="True"
        Background="Transparent"
        Title="Линейка"
        Icon="/ruler;component/images/ruler.ico"
        Topmost="{Binding TopMost}"
        WindowStyle="None"
        WindowStartupLocation="Manual"
        Left="{Binding Left, Mode=TwoWay}"
        Top="{Binding Top, Mode=TwoWay}"
        Width="{Binding Width, Mode=TwoWay}"
        Height="{Binding Height, Mode=TwoWay}"
        MinWidth="20"
        MinHeight="20"
        d:DesignWidth="800"
        d:DesignHeight="80"
        SnapsToDevicePixels="True">

    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="{Binding ResizeBorder}"
                      CaptionHeight="0" CornerRadius="0" GlassFrameThickness="0"
                      UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <local:EnumBooleanConverter x:Key="enumToBool"/>
    </Window.Resources>

    <Grid>
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Угол" IsCheckable="True" 
                          IsChecked="{Binding AngleVisible, Mode=TwoWay}"
                          InputGestureText="A"/>
                <MenuItem Header="Лупа" IsCheckable="True" 
                          IsChecked="{Binding MagnifierVisible, Mode=TwoWay}"
                          InputGestureText="Q"/>
                <Separator/>
                <MenuItem Header="Единицы">
                    <MenuItem Header="Пиксели" IsCheckable="True"
                              IsChecked="{Binding ScaleUnits, Mode=TwoWay,
                                Converter={StaticResource enumToBool}, ConverterParameter=Pixel}"
                              InputGestureText="1"/>
                    <MenuItem Header="DIP" IsCheckable="True"
                              IsChecked="{Binding ScaleUnits, Mode=TwoWay,
                                Converter={StaticResource enumToBool}, ConverterParameter=DIP}"
                              InputGestureText="2"/>
                    <MenuItem Header="Пункты" IsCheckable="True"
                              IsChecked="{Binding ScaleUnits, Mode=TwoWay,
                                Converter={StaticResource enumToBool}, ConverterParameter=Point}"
                              InputGestureText="3"/>
                    <MenuItem Header="Сантиметры" IsCheckable="True"
                              IsChecked="{Binding ScaleUnits, Mode=TwoWay,
                                Converter={StaticResource enumToBool}, ConverterParameter=CM}"
                              InputGestureText="4"/>
                    <MenuItem Header="Дюймы" IsCheckable="True"
                              IsChecked="{Binding ScaleUnits, Mode=TwoWay,
                                Converter={StaticResource enumToBool}, ConverterParameter=Inch}"
                              InputGestureText="5"/>
                    <MenuItem Header="Проценты" IsCheckable="True"
                              IsChecked="{Binding ScaleUnits, Mode=TwoWay,
                                Converter={StaticResource enumToBool}, ConverterParameter=Percent}"
                              InputGestureText="6"/>
                </MenuItem>
                <MenuItem Header="Ориентация">
                    <MenuItem Header="Горизонтальная" IsCheckable="True"
                              IsChecked="{Binding Orientation,Mode=TwoWay, 
                                Converter={StaticResource enumToBool}, ConverterParameter=Horizontal}"
                              InputGestureText="H"/>
                    <MenuItem Header="Вертикальная" IsCheckable="True"
                              IsChecked="{Binding Orientation, Mode=TwoWay, 
                                Converter={StaticResource enumToBool}, ConverterParameter=Vertical}"
                              InputGestureText="V"/>
                </MenuItem>
                <MenuItem Header="Нулевая точка">
                    <MenuItem Header="Слева (сверху)" IsCheckable="True"
                              IsChecked="{Binding ZeroPoint, Mode=TwoWay,
                                Converter={StaticResource enumToBool}, ConverterParameter=Near}"
                              InputGestureText="["/>
                    <MenuItem Header="Справа (снизу)" IsCheckable="True"
                              IsChecked="{Binding ZeroPoint, Mode=TwoWay,
                                Converter={StaticResource enumToBool}, ConverterParameter=Far}"
                              InputGestureText="]"/>
                </MenuItem>
                <MenuItem Header="Шкала снизу (слева)" IsCheckable="True"
                          IsChecked="{Binding Flip, Mode=TwoWay}"
                          InputGestureText="S"/>
                <MenuItem Header="Узкая шкала" IsCheckable="True"
                          IsChecked="{Binding ThinScale, Mode=TwoWay}"
                          InputGestureText="N"/>
                <Separator/>
                <MenuItem Header="Установить маркер" Command="{Binding SetMarkerCommand}"
                          InputGestureText="M"/>
                <MenuItem Header="Удалить маркер" Command="{Binding RemoveMarkerCommand}"
                          InputGestureText="D"/>
                <MenuItem Header="Удалить все маркеры" Command="{Binding ClearMarkersCommand}"
                          InputGestureText="C"/>
                <Separator/>
                <MenuItem Header="Окно">
                    <MenuItem Header="Поверх всех окон" IsCheckable="True"
                                IsChecked="{Binding TopMost, Mode=TwoWay}"
                                InputGestureText="P"/>
                    <MenuItem Header="Свернуть"
                                Click="Minimize_Click"
                                InputGestureText="–"/>
                    <MenuItem Header="На всю ширину (высоту) экрана"
                              Command="{Binding MaximizeRulerCommand}" 
                              InputGestureText="+"/>
                    <MenuItem Header="Восстановить"
                              Command="{Binding RestoreRulerCommand}" 
                              InputGestureText="Backspace"/>
                    <MenuItem Header="Закрепить на левой стороне экрана"
                              Command="{Binding DockRulerCommand}" 
                              CommandParameter="Left"
                              InputGestureText="L"/>
                    <MenuItem Header="Закрепить на верхней стороне экрана"
                              Command="{Binding DockRulerCommand}" 
                              CommandParameter="Top"
                              InputGestureText="T"/>
                    <MenuItem Header="Закрепить на правой стороне экрана"
                              Command="{Binding DockRulerCommand}" 
                              CommandParameter="Right"
                              InputGestureText="R"/>
                    <MenuItem Header="Закрепить на нижней стороне экрана"
                              Command="{Binding DockRulerCommand}" 
                              CommandParameter="Bottom"
                              InputGestureText="B"/>
                    </MenuItem>
                <MenuItem Header="Настройки..." Click="ShowOptionsDialog_Click"
                          InputGestureText="O"/>
                <Separator/>
                <MenuItem Header="Выход" Click="Exit_Click"
                          InputGestureText="Esc"/>
            </ContextMenu>
        </Grid.ContextMenu>

        <local:Scale x:Name="ruler"
                     FontFamily="{Binding FontFamily}"
                     FontSize="{Binding FontSize}"
                     MarkerFontSize="{Binding MarkerFontSize}"
                     ShortAxis="{Binding ShortAxis, Mode=OneWayToSource}"
                     ColorTheme="{Binding ColorTheme}"
                     Opacity="{Binding Opacity}"
                     ShowBorders="{Binding ShowBorders}"
                     ThinScale="{Binding ThinScale}"
                     Orientation="{Binding Orientation}"
                     ScaleUnits="{Binding ScaleUnits}"
                     ZeroPoint="{Binding ZeroPoint}"
                     Flip="{Binding Flip}"
                     TrackPoint="{Binding TrackPoint}"
                     Markers="{Binding Markers}"/>

        <!--<TextBlock HorizontalAlignment="Left" VerticalAlignment="Bottom">
            <Run Text="{Binding Width}" />
            <Run Text="{Binding Height}" />
        </TextBlock>-->

    </Grid>

</Window>
